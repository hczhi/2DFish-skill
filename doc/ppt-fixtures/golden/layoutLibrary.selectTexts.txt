L1 hero-right-visual（右侧全高视觉）
归属：内容 · 形状：分屏
适用：强观点页 / 金句页 / 单点强调
结构：左侧 54% 文字区（kicker+标题+金句+正文），右侧 46% 全高背景图（绝对定位 right:0 top/bottom:0），左向浅色渐变过渡。
图槽位：1 个（hero）。
fullbleed：否

L2 fullbleed-bg-mask（背景图铺满+浅色遮罩）
归属：封面 / 章节 / 结尾 · 形状：聚焦
适用：章节封面 / 总结页 / 过渡页
结构：整页背景图 + 浅色遮罩（linear-gradient 底→顶不透明），标题浮于浅色区。
图槽位：1 个（bg）。
fullbleed：否

L3 case-hero（案例 hero）
归属：内容 · 形状：分屏
适用：企业案例页
结构：右侧 64% 全屏大图 + 左侧渐变过渡；左白区放 72px 衬线企业名 + 关键词橙字下划线 + 要点。
图槽位：1 个（hero，建议半写实 mockup 模式）。
fullbleed：否

L4 two-col-cards（双卡左橙右蓝）
归属：内容 · 形状：对比
适用：对比 / 双要点
结构：两列卡片，左橙右蓝，各自标题+要点。
图槽位：0–2（可选小图标）。
fullbleed：否

L5 stat-grid（数据网格）
归属：内容 · 形状：数据
适用：数据页 / 指标罗列
结构：2×2 或 1×3 大数字卡，数字橙/蓝强调。
图槽位：0（用 CSS 数字，扁平信息图模式可选配图）。
fullbleed：否

L6 timeline-h（横向时间轴）
归属：内容 · 形状：时序
适用：演进 / 历程 / 阶段
结构：横向轴线 + 节点卡片。
图槽位：0–1。
fullbleed：否

L7 quote-full（金句整页）
归属：章节 / 结尾 / 内容 · 形状：聚焦
适用：过渡 / 强调
结构：居中大号衬线金句，关键词橙高亮，极简。
图槽位：0（或 1 张极淡背景）。
fullbleed：否

L8 gallery-grid（图廊网格 · 多图）
归属：内容 · 形状：并列
适用：多截图 / 多案例同屏
结构：`.gallery.g2`(1×2) / `.g3`(1×3) / `g2x2`(2×2) 网格，每格一张图+说明。
图槽位：2–4 个（g1..g4）。
fullbleed：否

L9 before-after（前后对比 · 多图）
归属：内容 · 形状：对比
适用：效果对比 / 改造前后
结构：左右两图 + 中间"→" + 上下标签（改造前/后）。
图槽位：2 个（before / after）。
fullbleed：否

L10 icon-row（图标行 · 多图）
归属：内容 · 形状：并列
适用：能力清单 / 特性罗列
结构：横排 N 个图标卡（图标+标题+一句）。
图槽位：N 个（小图标，≤200px）。
fullbleed：否

L11 tri-narrative-photo（三栏并列叙事 · 全幅图叠字）
归属：内容 · 形状：并列
适用：路线/策略三并行轨道；三阶段演进并列；三案例同屏对比；产品/服务三模块矩阵；三档价位/三能力等级。
结构：全幅三栏照片占满（`tri-cols` 1fr×3，inset:0）。统一页眉 `.slide-header` 浮在左上角；正文标题 `.tri-title` 浮在照片区上方（统一页眉下方，白色衬线大标题）。每栏：全幅照片 + 底部双向黑色渐变（to-top 主 + to-right 辅）+ 右上**衬线数字锚点（13…
图槽位：3 个（`pXX_col1/col2/col3`，建议 case/concept 模式，3:4 竖版 1024×1365，禁用 data）。
fullbleed：是
提示：视觉重，前后页用 L7/L2 缓冲；副标 4~8 字；数字必须衬线。

L12 circle-float-card（圆形浮卡 · 出血色带）
归属：章节 / 内容 · 形状：聚焦
适用：人物/嘉宾/创始人介绍页；**产品介绍页**（圆形产品图）；项目/案例展示扉页；章节扉页；获奖/荣誉展示；客户成功故事开场。圆形视觉主体可以是头像、产品图、logo、图标——任何圆形 crop 的视觉元素。
结构：外层浅底 inset `56px 56px 90px` + 白色浮卡（圆角 24px）+ 内分栏 1.4fr/1fr（文本/圆形视觉）。文本区五层堆叠：灰 kicker → 品牌色巨字(116px serif) → 深色中字 → 灰色描述段 → 品牌色短线。右侧圆形视觉(320px) + 6px 白边 + 阴影。底部品…
图槽位：1 个（`pXX_portrait`，必填，正方形 1080×1080，case 模式——人像/产品/实物均可，圆形 crop 后无信息损失）。
fullbleed：否
提示：视觉中等偏重，前后页用 L7/L2 缓冲；super-word ≥96px 必须衬线；色带贴在 card 内底部（不出血，出血那一半会被 slide 裁掉）；圆形视觉选材需清晰正面/主体居中。

L13 fullbleed-stack-center（全幅图叠字 · 居中垂直堆叠）
归属：封面 / 章节 / 结尾 · 形状：聚焦
适用：封面页（年度报告/季度发布/主题发布）；扉页（章节开场/节目标签/主讲人开场）；宣言页（战略宣言/主题金句/行业承诺）；双语发布页（跨境发布/国际合作/海外项目启动）；倒计时页（活动倒计时/上线启动/周年纪念）；章节分隔页（长 deck 中的章节扉页）。
结构：全幅背景图 + **中心径向亮边缘暗渐变**(`ellipse 80% 70% at center`, 非纯圆形) + 文字垂直居中堆叠（年份大字轻量 → 主标题大字含引号 → 中文副标一行 → 双语脚注 中上英下） + 顶部右上胶囊标签(毛玻璃+白字)。核心区别于 L11（底部单向黑色渐变横向分栏）。
图槽位：1 个（`pXX_hero`，必填，必须是"景深感"全幅场景图——山区航拍/跨海大桥/星空/城市远景，不要人物 closeup 或抽象插画）。
fullbleed：是
提示：视觉重(封面压场感)，前后页用 L7/L2/L12 缓冲；引号包裹主题词是结构信号(标题必须有"被引用感")；配色不固定骨架遵循 deck palette；连续多页用变体避免重复。

L14 vertical-split-hero-info（上下分屏 · 上 hero 全幅图 + 下双栏信息）
归属：内容 · 形状：分屏
适用：产品介绍页（新能源车/概念机/品牌发布主图）；项目详情页（地产楼盘/工程项目/技术架构图）；案例分析页（客户案例/标杆项目深度剖析）；章节配图页（长报告"本期特写"/"本章案例"）；嘉宾/讲者深度页（带 hero 视觉+下方双语履历）；解决方案详情页（方案封面+下方能力详解）。
结构：上下二段式 flex 分屏（上半 56% hero 图 + 下半 44% 浅底双栏信息）。上半：全幅 hero 图 + **由上往下的黑色渐变**（顶部最暗，向下渐隐）+ 左对齐文字块（kicker→英文大字→中文大字）**位于 hero 下部（约 62% 高度处）**，并加投影保证可读。下半：`var(--c-bg)…
图槽位：1 个（`pXX_hero`，必填，case 模式场景/产品实景图，1920×1080，`object-position: center 40%`，禁用人物 closeup/抽象插画/数据图表）。
fullbleed：否
提示：视觉重(hero 图压场)，前后页用 L7/L13/L12 缓冲；56/44 可微调但 hero 不低于 40%；hero 文字 48~64px；下半小标题必须有 hairline 底线；与 L3 区别——L3 是"左文右图"单段式，L14 是"上图下信息"双段式，信息密度更高。

L15 fullbleed-photo-thin-text-tri-col（上 hero 极小字+纯白三栏文字·克制美）
归属：内容 · 形状：并列
适用：品牌价值观页（三大理念/三大原则/三大支柱）；方法论三要素；产品三大优势（商务克制版，比 L4 双卡更"高级"）；服务三大承诺；团队/业务三大方向。
结构：上下分屏 40/60：上半 hero 全幅图（占 40%）+ 左上 logo+中文标语 2 行 + 右上小说明 2-3 行（**字号极克制 16-20px，不抢戏**）；下半纯白三栏等宽 + 每栏 `│` 竖线锚点 + 中文小标(粗)→中文段(17px)→英文段(11px) **中大英小节奏 1.5:1**。**无渐变…
图槽位：1 个（`pXX_hero`，必填，case 模式氛围场景图——走廊/钢架/远眺/海面，构图简洁有"景深感"，1920×1080，`object-position: center 40%`，禁用人物 closeup/抽象插画）。
fullbleed：否
提示：商务克制美（与 L11 商业路演风相反）；hero 字号 16-20px 是关键气质（**不要**抄 L13 的 96px）；英文段与中文段字号比 11:16 ≈ 0.69（别用 1:1）；竖线只贴合中文小标高度（≈22px），不要长竖线。

L16 quad-city-matrix（顶 header 极小 + 标题区 + 四栏等宽「图+中英标题+双语描述」单元）
归属：内容 · 形状：并列
适用：城市/办公地分布；客户案例矩阵（4 家同屏）；产品矩阵（4 款同屏）；业务四大板块；门店/分公司矩阵；合作伙伴矩阵；四季/四节内容；四类用户/四类场景。
结构：标题区（中文大标 + 中文一行副标）+ 主体四栏矩阵（74%）。**顶部/左右留白全部由 `.slide-inner` 给**（`--pad-top` 里已经含了页眉那条带），这一层不要自己写 padding、也不要用 inline style 改它 —— 改小了标题会被左上角那行模块名压住（页眉在最上层），而页面照样…
图槽位：N 个（`pXX_c1..c4`，必填，case 模式场景/建筑/产品图，**所有图色调统一**，建议 3:4 或 4:5 竖版 800×1066，**严禁混搭构图风格**）。
fullbleed：否
提示：纸面陈列风（与 L11 商业路演、L15 商务克制都不同）；顶 header 字号必须 ≤ 14px（克制气质的核心）；图色调统一是关键（建议 `filter:saturate(.92) contrast(1.05)`）；单元高度差 ≤ 8px。

L17 left-image-right-toc-circles（左 50% 全幅图叠中英巨字 + 右 50% 米底圆圈数字目录）
归属：章节 · 形状：分屏
适用：章节扉页（强气质版，5 个以内章节）；品牌系列入口（5 款产品/5 条主张/5 个故事）；跨主题导览页。
结构：横向 50/50 分屏（中间无明显分界，靠色温切换）。**左 50% 全幅图**：中央偏上白色毛笔字中文巨字（200–280px，靠 `STKaiti`/`KaiTi` 楷体还原笔触感）+ 图底部 1/3 浮英文大写小字（48–64px，半透/描边）。**右 50% 浅米底**：顶部右胶囊 pill + "CONTEN…
图槽位：1 个（`pXX_hero`，必填，**场景图为主**——人在路上/城市航拍/山野远眺，禁用纯产品 closeup 与抽象插画，1920×1080）。
fullbleed：否
提示：视觉中重；中文毛笔字必须用 `STKaiti`/`KaiTi`/`KaiTi_GB2312` 衬线楷体（不能换无衬线）；圆圈描边 ≥ 1.5px + 数字字重 ≥ 700（描边太细会糊）；`mix-blend-mode: screen` 让毛笔字自动适配底图明暗；描述段行数 ≤ 4 行（多了会破坏右半视觉平衡）。

L18 bg-overlay-bilingual-headline-wavy（全幅暗 bg + 英文左 / 中文右双标题对称 + 红色波浪线 + 暗蒙版三栏）
归属：章节 / 内容 · 形状：聚焦
适用：章节扉页 / 概念页（议题有"双语对照感"——中英两版标题并置）；品牌主张/宣言页；议题页（用 3 支撑点拆解一句话主张）。
结构：全幅暗 bg（**深色氛围**——森林光束/深山/星空/暗海）。**顶部不保留 `.l18-logo` 和 `.l18-pill`**，模块名统一由 `.slide-header` 承载。顶部 1/3 横向双标题：**左侧英文巨字**（2 行 72–96px 粗体白色）+ **右侧中文巨字**（2 行 88–112px…
图槽位：1 个（`pXX_hero`，必填，**深色氛围图**——森林光束/暗海面/星空/雾林，禁用浅色明亮图与产品 closeup，1920×1080）。
fullbleed：是
提示：视觉重（暗 bg + 大字 + 暗蒙版三栏，气场最强之一）；暗 bg 上禁止用 `var(--c-ink-soft)` 浅灰（会糊），所有正文必须 `rgba(255,255,255,.78)` 带透明度的白色；双标题中英对仗工整（行数对齐、字号比例约 1:1.2）；波浪线穿过中文的位置必须在标题中央（top:50–6…

L19 fullbleed-product-feature-trio-spec（全幅暗 bg 产品图 + 左上标题 + 中央产品 + 底部 3 白卡参数）
归属：内容 · 形状：聚焦
适用：产品参数/规格页（强产品风，3 个核心参数一目了然）；产品功能介绍页（单产品 + 3 卖点）；设备/器材/技术规格页（相机/镜头/汽车/工具）。
结构：全幅暗 bg（**产品在场景中**——飞行器飞过山谷/汽车行驶公路）。顶部 logo + 小标 → **左上**主标（48–64px "Wide-Angle Camera" 浅色粗体）+ 副标（14–18px pipe 分隔技术列表）→ **中央偏左 1/3** 产品图（PNG 透明底 35–50% 宽，与背景融合）→…
图槽位：2 个（`pXX_hero` 必填，**产品在场景中的全景** 1920×1080，禁用纯产品白底图与人物特写；`pXX_product` 必填，**产品 cut-out 图**，**PNG 透明底最佳**，1600×900 宽高比约 16:9，JPG 抠图需用 `mix-blend-mode: lighten` 配合）。
fullbleed：是
提示：视觉重（暗 bg + 产品大图 + 白卡对比，气场强）；3 白卡必须等宽 + 间距一致；卡片参数主标字号 ≥ 24px（太小大屏会糊）；产品图必须 PNG 透明底；左上标题与产品图不能视觉打架（产品图位置避开头标题区域）；底部白卡与暗 bg 的对比是版式灵魂，禁止改半透卡。

L20 left-textband-wide-hero-icon-cluster（左 30% 浮文带 + 右 70% 宽 hero + 底部三圆图标）
归属：封面 / 章节 / 内容 · 形状：分屏
适用：战略/价值观页；单一强势主张 + 时间锚点 + 三支撑点；章节扉页 / 报告封面；品牌主张页（"X 年 X 事，3 个支柱"）；企业能力速览页。
结构：横向分割 **左 30% 文字带 + 右 70% 全幅图**（非出血，文带用 `linear-gradient(to right, var(--c-card) 75%, transparent)` 自然过渡）。**文字带顶部 padding 留足约 140px**，把主文字整体下移，避让统一页眉。文带五层堆叠：英巨字(…
图槽位：1 个（`pXX_hero`，必填，case 模式场景/产品/环境图，**主体偏右**因左 30% 文带会遮住左部，1920×1080）。
fullbleed：否
提示：视觉中等偏重（hero 占 70% 主导）；仅 `var(--c-brand)` 用于大数字与图标；三圆与正文间距 ≥ 48px。与 L1 区别——L1 是 54/46（文多图少），L20 是 30/70 + 三圆收尾。

L21 fullbleed-perspective-info-split（全幅中央透视 + 左文字 + 右三数据栏）
归属：内容 · 形状：数据
适用：能力/优势页；成果汇报页；数据展示页；"1 个焦点场景 + 3 个数据指标"；战略升级页；国际化数据发布页。
结构：**全幅图**（不包 `.slide-inner`）+ **双向收暗蒙版**（左 0–35% + 右 65–100%，中央透亮）+ 左 1/3 文字 + 右 1/3 三栏数据 **整体垂直居中**（`top:50% + translateY(-50%)`），并对所有文字加投影增强可读性。左文字 = 英巨 56–64px…
图槽位：1 个（`pXX_hero`，必填，case 模式**必须中央透视构图**——栈道/桥/船头/隧道/走廊延伸感，禁用人物 closeup/抽象插画）。
fullbleed：是
提示：视觉重；图**必须中央透视**（左右两侧自然偏暗被蒙版强化）；仅 `var(--c-brand)` 用于短线；数据用纯白衬线。与 L13 区别——L13 是居中垂直堆叠，L21 是横向分置（左文字+右数据）。

L22 stacked-hero-info-block-each-split（上下两段 + 每段各自左右分栏）
归属：章节 / 内容 · 形状：分屏
适用：品牌故事页；品牌概述页；双模块组合（"门面 + 内涵"）；设计师/手作品牌详情；章节扉页后接详情；产品材质 + 品牌理念组合页。
结构：**上下分屏**（顶底贴边，不包 `.slide-inner`），各 50%。上半内分左右 1:1：左 = 材质/产品大图 + 135deg 渐变 + 底部白色品牌字（中文 32–40px 细体 + 英文 18px letter-spacing 0.32em）；右 = 浅米底 + "Brand summary" 小标题…
图槽位：2 个（`pXX_hero` 上半大图，必填，材质/产品/品牌主视觉，暖色调；`pXX_sub` 下半左副图，可选，与 hero 同色系）。
fullbleed：是
提示：视觉中等；上下色温差异是版式气质核心；上半 hero 字 32–40px 偏细体（与 L13 的 96px 衬线重磅形成对比）；下半小标 18px + hairline 底线是气质信号。与 L14 区别——L14 是"上 hero + 下双栏"单段式，L22 是"上下两段 + 每段各自左右分"双段式，信息密度更高。

L23 fade-bg-left-title-stack（整页背景图横向渐隐 + 左侧标题/主体两段堆叠）
归属：封面 / 结尾 · 形状：聚焦
适用：整份封面（公司/项目/方案封面）；结尾致谢页 / 联系方式页；单张主张页（一句主标题 + 署名）。
结构：整页背景图 + **横向**渐隐（90deg，左侧压成页面底色、右侧透出画面，视觉重心必须在右半边）；左栏（≤58% 宽，在 `.slide-inner` 内垂直居中）堆两段：标题段（82px 主标题 + 28px 品牌色副标题）和主体段（22px 署名 + 一枚浅底胶囊标签），两段之间留 96px 空白。深色字压在渐…
图槽位：1 个（bg，concept 模式，浅色调、**视觉重心在右半边**、左 1/3 留浅 —— 暗底图会让左侧的深色主标题读不出来）。
fullbleed：否
提示：视觉重量高，前后不要再接全幅图页（L2 / L13 / L21）；横向渐隐是签名，改成纵向就退化成 L2；两段之间那 96px 空白是节奏，填满之后这一条就变成一页普通图文页；与 L13 的区别——L13 暗底居中白字（气势），L23 浅底左对齐深字（商务克制）。

L24 quad-matrix-conclusion（报告风标题条 + 2×2 象限卡 + 底部结论条）
归属：内容 · 形状：对比
适用：四种场景 / 四类用户 / 四个象限的分类页；「一页服务不了几种场景」这类判断页；风险与对策矩阵；四类内容的取舍页。
结构：顶部报告风标题条（`.rp-head`：`h1.page-title` + 2px 深色横线 + 可选一行 `lead`）→ 中部 2×2 等高卡（每格 = **铺满整格的底图**（`.l24-bg`，压在 `.93→.66` 蒙版下）+ 小标题 + 2–3 行正文，**其中一格 `.on` 品牌色描边高亮**）→ 底…
图槽位：4 个（`pXX_bg1..bg4`，可选但要么全有要么全无，case 模式 16:9 **每格的底图，画面要平、主体别压左上角、四张同一路调性**）。
fullbleed：否
提示：视觉中等（四张底图压在蒙版下）；**高亮格只能一格**（两格以上就退化成「四个彩框」）；与 L4 的区别 —— L4 是两块对立，L24 是同一维度四种取值 + 一格指向；与「数据网格」那一条的区别 —— 那一条格子里放的是大数字（**这里按名字写、不写编号**：停用一条版式时它的编号不能出现在选型 prompt 的任…

L25 axis-icon-nodes（报告风标题条 + 横轴四节点「上标签 / 圆形图片 / 下描述」+ 结论条）
归属：内容 · 形状：时序
适用：阶段推进页（活动前/中/后）；生命周期页；四步流程页；「同一件东西在不同阶段扮演不同角色」这类时序判断页。
结构：顶部报告风标题条 → 中部一条贯穿细横线，四个 128px 圆压在线上（图**铺满整个圆**，`cover` + 圆形裁切），线**上方**阶段名（28px 粗）、线**下方**一句描述（19px ≤2 行），**末节点 `.on` 高亮**（品牌色圆环 + 暖色光圈）→ 底部结论条。三层严格对齐是签名。
图槽位：4 个（`pXX_n1..n4`，可选但要么全有要么全无，case 模式 1:1 **铺满圆的近景图，主体正中、四角可丢、四张同一路调性**）。
fullbleed：否
提示：视觉中等偏轻（可放在两页密集页之间当缓冲）；**阶段名必须一行**（高度写死 40px，换行会把整条轴线顶歪）；**圆的直径和 `top:160px` 绑在一起**（改直径忘了改它，线会横穿图片）；轴线用 `var(--c-ink-deep)` + `opacity:.28`（`var(--c-hairline)` 在…

L26 contrast-duo-quote-icon-row（对比双条（浅底 vs 反色）
归属：内容 · 形状：对比
适用：一对概念的区分页（主题 vs 模板、平台 vs 应用）；「区分 → 判断 → 展开」三层递进页；定义页 + 可复用层次清单。
结构：三层由重到轻 —— 标题条 → **对比双条**（左浅底 `.l26-a` / 右反色 `.l26-b` 深底白字，反色那条读成「更上层的那一个」）→ **竖线引用**（5px 品牌色竖线 + 30px 粗体一句，把区分收成判断）→ 底部一行小灰字 + **四栏图卡**（每格 = 铺满卡片的底图（上淡下浓蒙版）+ 层次…
图槽位：4 个（`pXX_bg1..bg4`，可选但要么全有要么全无，case 模式 16:9 **每格的底图，主体在上半张、四张同一路调性**）。
fullbleed：否
提示：视觉中等偏重（这一族里最满的一条，前后用 L2 / L7 / L13 缓冲，别接 L24 / L28）；**反色那一条是结构签名**（两条都浅底之后「包含关系」这层意思消失）；引用层只写一句；暗底正文用 `rgba(255,255,255,.8)`，不要用 `var(--c-ink-soft)`（会糊）；与 L4 的区…

L27 checklist-tall-figure（报告风标题条 + 左侧编号清单 + 右侧整列竖图）
归属：内容 · 形状：分屏
适用：判据清单页（怎么判断做到了）；操作要点页；3–5 条有顺序的要求；带一张现场竖图的方法页。
结构：顶部报告风标题条（`.rp-head` + 可选一行 `lead`）→ 下方左右两列（1fr / 560px）：左列 3–5 条编号清单（大号品牌色编号 + 小标题 + 1–2 行说明，条间细分隔线，末条无线），右列一张**贴顶贴底的竖图**（靠 grid 行拉伸撑满，不写高度）+ 下缘一行深色渐变说明。
图槽位：1 个（`pXX_fig`，可选，case 模式 3:4 **竖构图、主体居中偏上，下缘 26% 会被说明条压暗**）。
fullbleed：否
提示：视觉中等；**编号是结构签名**（去掉之后只是「右边有张图的正文页」，顺序感消失）；图**不要写死高度**（清单一多右下角就空一块，而页面照样渲染）；前后别接同样左右分屏的 L1 / L3；与「横排并列清单」那一条的区别 —— 那一条是 N 项平等并列，这一条是纵向有序。

L28 dual-col-checklist-photo-band（报告风标题条 + 双列勾选清单（6–8 项）
归属：内容 · 形状：并列
适用：交付自检清单；验收标准 / 准入条件；6–8 条可逐项核对的一句话；发布前检查页。
结构：顶部报告风标题条 + 一行 `lead`（这几条什么时候用）→ 中部双列勾选清单（`.l28-cols`，**项数必须偶数**，每项 = 圆形勾选点 + **一行**文字、关键半句 `<b>`，行距固定 30px 紧凑排在标题下面）→ 底部横幅图带（`flex:1` 吃掉页面剩余高度、最矮 216px，蒙版**左浓右淡…
图槽位：1 个（`pXX_band`，可选，case 模式 16:9 **切成约 1640×400 的横条，主体必须在右半张**，左边被蒙版压住给文字）。
fullbleed：否
提示：视觉偏重（八行字），前后用全幅图页缓冲、**别接 L24 / L26**；**每项只写一行**（两行之后图带被挤到贴着清单，这一页从「清单」变成「一大段字」）；**项数偶数**（奇数时右下角空一格，看着像漏了一条）；清单项之间不要有强顺序（grid 是左右左右读的）；图带上的字直接写 `#fff`，不要用 `var(-…

L29 dark-statement-flow-loop（全幅暗底金句 + 底部四步回环）
归属：内容 / 结尾 · 形状：时序
适用：一段论证的收口页；工作方式 / 方法论页；结尾金句 + 可循环的四步做法；理念页。
结构：整页暗底（背景图 `opacity:.4` + 径向暗蒙版）→ 中部（`.l29-say`，`top:24%`）一句 64px 衬线金句（`h1.page-title`，关键几字 `<em>` 染品牌色）+ 一行 21px 补充 → 底部四步回环（四个半透明描边格，格间 `→`，**末格是 `↺`** = 回到第一步）…
图槽位：1 个（`pXX_bg`，可选，concept 模式 16:9 **整页气氛底图，大面积低对比、不能带文字**，会被压到 40% 亮度）。
fullbleed：是
提示：视觉重（整页暗底），整份里这类暗页别超过两三页、**不要和 L18 / L19 / L21 相邻**（连着看像没翻页）；**末格 `↺` 是结构签名**（换成 `→` 就退化成单向四步流程，那种用 L6）；暗底正文一律 `rgba(255,255,255,.72)`、描边 `rgba(255,255,255,.22)`…

L30 numbered-evidence-trio（三栏编号证据卡：上图下文，三栏同色）
归属：内容 · 形状：并列
适用：三处真实改动；三个案例 / 三条证据（每条都有变化量）；前后对比的三个切面；成效页。
结构：顶部报告风标题条 → 三张等高证据卡：上部 62% 高的图（左下角压一个品牌色编号块）+ 下部小标题 + 2–3 行 + 一行**收口数据**（`margin-top:auto` 贴卡底 + 上分隔线）。**三栏配色完全一样**（谁最重要靠那行变化量说）。
图槽位：3 个（`pXX_e1..e3`，可选但要么三张全有要么全无，case 模式 16:9 **主体居中、左下角留白给编号块，三张同一路调性**）。
fullbleed：否
提示：视觉中等偏重，前后接 L2 / L13 / L25 缓冲；**三栏配色完全一样**（原来末栏有个 `.dark` 反色变体，已去掉；**别再给某一栏套深底 inline style** —— 那一栏在整份浅色稿子里是唯一一处深底卡，翻起来像另一套模板里的页，而这一页自己看完全正常）；**收口数据三栏都要有**（缺一行就…

L31 photo-band-float-card（全幅：上部浅底标题 + 下部横贯图带 + 压在图上的浮白卡）
归属：章节 / 内容 · 形状：聚焦
适用：章节扉页 / 过渡页；"一个主张 + 一个证据"页；带一个量化指标的观点页；产品或场景引入页。
结构：上部浅底标题区（58px 标题 + 一段引导，内边距走 `var(--pad-*)`）→ 下部一条**横贯到页边的图带**（`flex:1` 吃掉剩余全部高度，左浓右淡暗蒙版）→ 一张 660px 浮白卡（品牌色顶边 + 大投影）压在图带左下，右下角一个 64px 白色大数字 + 一行口径。
图槽位：1 个（`pXX_band`，**必填**，case 模式 16:9 **主体在右半张、左边留大面积低对比区给白卡、右下角别有细节**）。
fullbleed：是
提示：视觉中等（上浅下图），适合当章节页或密集页之间的缓冲；**白卡压在图带上是结构签名**（挪进 `.l31-band` 里会被 `overflow:hidden` 连投影一起切掉，看起来像「卡片没浮起来」）；白卡 `left` 必须是 `var(--pad-x)`（写 0 的话和标题差 140px，画面上很难一眼指出来）…

L32 mosaic-hero-duo-overlay（全幅三图拼贴：左一张主图通高 + 右两张辅图，文字压在各自图上）
归属：章节 / 内容 · 形状：并列
适用：同一内容的三个版本 / 三个侧面；一主两辅的场景页；章节扉页（图片气质重）；作品或案例拼贴页。
结构：整页三图拼满（左 `1.34fr` 通高 + 右上/右下各一张，6px 缝隙露出 deck 底色）；三块统一一层**上淡下浓**蒙版（`.1 → .86`），文字压在各自图块**下缘**：左块 = kicker + 56px 衬线 `h1.page-title` + 一段说明，右两块各 = 小标题 + **一行**说明…
图槽位：3 个（`pXX_a` 必填 case 1:1 **主体在上半张**；`pXX_b` / `pXX_c` 必填 case 16:9 **主体在上半张、两张同调性且别和主图撞构图**）。
fullbleed：是
提示：视觉重（整页三图），**不要和 L11 / L13 / L21 / L22 相邻**；**左图通高是结构签名**（三块等分就和「三图等宽叙事」那一条重了）；**三块都要蒙版**（少给一块，那一块的白字在亮图上直接看不见，而另两块完全正常，看起来像「这张图选得不好」）；**辅图说明必须一行**（两行之后辅图只剩顶上一条边…

L33 toc-rows-side-photo（目录页：左侧编号行表 + 右侧整列竖图）
归属：章节 · 形状：分屏
适用：**默认停用**（migration 103 —— 稿子里不排 agenda 页了）。要用得先在案例库页面上手动启用；原来的用法是目录页 / 导览页 / "本次要讲的 N 件事"。
结构：顶部报告风标题条（标题写「目录」这类）→ 下方左右两列（1fr / 580px）：左列 4–6 行编号行表（每行三格 `100px 1fr auto` = 编号 + 33px 章节名 + 右端页数/时长，行间细横线 + **第一行还有一条上边框**），**当前那一行 `.on` 转品牌深色**；右列一张贴顶贴底的竖图 …
图槽位：1 个（`pXX_fig`，可选，concept 模式 3:4 **竖构图气氛图，主体居中偏上**；目录页的图不承担信息）。
fullbleed：否
提示：视觉轻（一页导览），一般是整份第 2 页；**`.on` 只能一行**（多了就没有指向）；**章节名必须一行**（换行之后几条横线的间距全乱，而页面照样渲染）；右端那一格 `<span>` 不能省（省了右端塌陷、各行参差）；最多 6 行；与「诗意导览」那一条（毛笔字 + 圆圈目录）的区别 —— 那条气质重，这条是报告风…

L34 cover-slat-photos-title-band（封面：三条不等宽竖图幕帘 + 横贯整幅的白色标题带）
归属：封面 · 形状：聚焦
适用：整份第 1 页封面；年度/季度汇报开场；主题发布会封面；有主题词可以强调的标题。
结构：整幅被切成三条**不等宽**竖图（`1.18fr / .74fr / 1.08fr`，缝隙 8px，像一排幕帘）→ 一条**横贯整幅**的白色标题带压在上三分之一（`top:33%`，顶边 6px 品牌色）：细线 eyebrow（英文小字 + 自动延伸的细线）+ 82px 衬线主标题（`<em>` 给主题词上品牌色）+…
图槽位：3 个（`pXX_slat1..slat3`，可选但要么三张全有要么全无，concept 模式 3:4 **竖构图、主体在中上部、三张同一路调性**）。
fullbleed：是
提示：**只当整份第 1 页**（没有页眉、也没有留给正文的空间）；视觉重，第 2 页必须换轻的那一档；**三条宽度必须不等**（`repeat(3,1fr)` 之后它就是一堵三宫格图片墙，封面的节奏没了）；**主标题最多两行**（三行会盖住底部那排元信息，而两者都还在渲染）；压在图上的元信息直接写 `rgba(255,25…

L35 cover-offset-block-portrait（封面：左侧标题 + 右侧品牌色块与竖图错位叠压）
归属：封面 · 形状：分屏
适用：整份第 1 页封面；报告/提案封面；克制、文字为主的开场；需要露出品牌色的封面。
结构：**三层错位叠压** —— 品牌色实块贴右边缘通高（宽 42%）→ 一张竖图往左下错开压在它上面（`right:200px`、上下各留 96px，带大阴影），色块只在右侧露出一条 200px 和上下两条 → 左半浅底上：顶部加宽字距英文 + 64×5px 品牌色短杠 + 78px 衬线主标题（`<em>` 主题词）+ …
图槽位：1 个（`pXX_fig`，可选，concept 模式 3:4 **竖构图、主体居中偏上、调性要和品牌色搭得上**）。
fullbleed：是
提示：**只当整份第 1 页**；视觉中等偏重（比 L34 轻一档，**同一份里两条封面只能选一条**）；**错位是结构签名**（图和色块对齐之后这一页就是「左文右图」，和 L1 / L3 撞了）；**文字块宽度别加**（42% 到竖图左缘只剩 44px，加宽之后标题末几个字压在照片上，颜色还是深的、读起来只是「有点脏」）；…

L36 closing-framed-thanks-contact（结尾：全幅暗底气氛图 + 中央细框巨字 + 底部三栏联系信息）
归属：结尾 · 形状：聚焦
适用：整份最后一页；致谢页 / 收场页；需要留联系方式或下一步动作的收尾。
结构：一张**压到 `.32` 的全幅气氛图**当底（再叠一层上下浓中段淡的纵向蒙版）→ 中央一个**1px 细线框**（左右各缩 16%，半透白底 `.04`）里居中三层：104px 衬线巨字（最多 4 个汉字）+ 加宽字距英文小字 + 2–3 行收尾话 → 底部细线上的**三栏联系信息**（单位 / 联系方式 / 日期）…
图槽位：1 个（`pXX_bg`，可选，concept 模式 16:9 **远景/夜景/抽象肌理，中段不要有主体**；**不配图也完整**，别为了凑图硬塞）。
fullbleed：是
提示：**只当最后一页**（中间用它会读成「讲完了」）；前一页别再放暗底页（L29 / L32 也是暗的，连着两页分不出这是最后一页）；**巨字最多 4 个汉字**（换行之后框被撑到盖住底部联系信息，而两者都还在渲染）；**细框不许加粗**（1px + `.26` 白是全部气质，2px 起就是横幅广告）；暗底上的字直接写 `…

L37 dense-two-col-prose（长文页：报刊双栏正文 + 首字下沉 + 右侧边注栏）
归属：内容 · 形状：分屏
适用：需要**读**而不是扫的长说明（背景介绍 / 立场陈述 / 方法论综述 / 政策解读）；**整库文字容量最大的版式**（两栏合计 24–28 行，约 900–1100 字）。
结构：双线报头（`3px double` + 右端两行 dateline 小字）→ **两栏正文**（栏间一条竖细线，第一栏第一段**首字下沉** 76px 衬线品牌色）→ 右侧 380px 边注栏（2px 品牌色竖线）：1:1 方图 + 加宽字距小标签 + 3–4 行边注 + 贴底的衬线摘句。
图槽位：1 个（`pXX_aside`，可选，concept 模式 1:1 **构图极简、主体居中、调性要安静**——这一页主角是文字）。
fullbleed：否
提示：**整份最多两页**（长文页连着两三页会让人合上电脑，中间夹一页 L31 / L32 / L13）；**别用 `column-count` 分栏**（浏览器分栏时超出的字直接消失，而页面看起来只是「最后一段没写完」）；**首字下沉只给第一栏第一段**（两栏都下沉会读成两篇不相干的文章）；**边注栏 380px 别改成 …

L38 thumb-row-list（图文行表：方缩略图 + 小标题 + 说明 + 右端编号，4–6 行）
归属：内容 · 形状：并列
适用：名词解释 / 条款说明 / FAQ / 分档说明 —— 每条都值得配一张小图的清单；文字容量大但**要点式**（4–6 行 × 小标题 + 2–3 行，约 600–900 字）。
结构：报告风标题条（`.rp-head`，建议带 `lead` 说清排序依据）→ 4–6 个横行，每行四格：**120px 方缩略图** → 小标题（可挂一行加宽字距英文 `<em>`）→ 2–3 行说明 → **右端 34px 灰色编号**；行间细线，末行不画线。
图槽位：4–6 个（`pXX_t1..t6`，可选但要么每行都有要么整页不放，concept 模式 1:1 **主体严格居中、构图极简、几行同一路调性**）。
fullbleed：否
提示：**行数只能 4–6 行**（`justify-content:center` 时七行起会**同时**从上下两端溢出，而中间那几行看起来完全正常）；**小标题 10 字以内**（320px 那一格换行会顶高整行、把别的行压缩）；**编号必须在右端**（挪到左边和缩略图抢「这一行从哪儿开始」，清单读起来是散的）；**缩略…

L39 dark-longform-vertical-title（暗底长文：左侧竖排中文标题 + 中间正文 + 右侧数据边栏）
归属：内容 · 形状：分屏
适用：需要**读**的暗底页：立场陈述 / 行业判断 / 一段有分量的综述 + 2–3 个支撑数字；文字容量大（正文 3–4 段共 14–18 行，约 700–900 字）。
结构：压到 `.22` 的全幅气氛图 + 左右浓中段淡的**横向**蒙版 → 三竖带：左侧 `writing-mode:vertical-rl` **竖排 56px 衬线标题**（+ 一行竖排英文小字，整库只有这一条这么排）、中间 21px / `line-height:2` 正文（垂直居中）、右侧 340px 数据边栏（半…
图槽位：1 个（`pXX_bg`，可选，concept 模式 16:9 **远景/肌理/夜景，中段不要有主体**；**不配图也完整**）。
fullbleed：是
提示：**整份最多一页**，且前后别接暗底页（L29 / L32 / L36 也是暗的，连着两页会分不出换了页）；**竖排标题只能 6–10 个汉字**（写长了从下缘静默裁掉，而目录面板里那一条是全的）；**正文别从 `--pad-x` 起**（会压在竖排标题上，两层都是白字，只表现成「有点糊」）；数据栏的数字必须是正文里出…

L40 diagonal-split-photo-text（斜切分屏：clip-path 对角线 + 左文右图 + 斜线渐变高光）
归属：内容 / 章节 · 形状：分屏
适用：一句判断 + 三条支撑，并且**有一张能撑起半页的实景图**（方案开篇 / 单个案例 / 能力介绍 / 合作说明）；文字容量中等（标题 + 引言 + 3 条，约 260–320 字）。
结构：整库唯一的**斜切**分屏 —— 右侧图块由 `clip-path:polygon(100% 0,52% 0,66% 100%,100% 100%)` 切成上宽下窄的斜梯形（自带暗底 + 165° 黑渐变），斜边上压一条 3px 品牌→accent 渐变高光；左侧 40% 通高文字块垂直居中（62px 衬线标题 + 引…
图槽位：1 个（`pXX_side`，可选，case 模式 16:9 **主体在画面右三分之二、右下角要能压深**；**不配图也完整**（留暗色斜块，V4））。
fullbleed：是
提示：**`clip-path` 的百分比和 `.l40-edge` 的 `left`/`rotate` 是同一条线的两份表达，改一个必须改另一个**（否则画面上是两条错开几十像素的斜线，看起来像"设计成双线"，一处都不报错）；**文字区别超过 42%**（标题会伸到暗图上，深墨字压暗图只表现成"这一行有点糊"）；**图注只…

L41 outline-mega-number-chapter（描边空心巨数字章节页：420px 空心序号 + 压在上面的衬线标题 + 底部三条导览）
归属：章节 · 形状：聚焦
适用：章节扉页（第几部分开始），底部三条导览写这一章接下来的三个小节；**纯排版不放图**，文字很少（约 90–140 字）。
结构：420px `var(--num)` 巨数字，`color:transparent` + **`-webkit-text-stroke:2px var(--c-brand)`** 只剩一圈品牌色轮廓（整库唯一）；92px 衬线章节标题**压在它右下方**（`left:calc(var(--pad-x) + 210px)…
图槽位：0 个（**这一条不放图** —— 衬任何一张照片那圈 2px 轮廓就断断续续看不出形状，而页面照样渲染，只表现成"这个数字有点脏"）。
fullbleed：是
提示：**`color:transparent` 不能少**（少了是一个 420px 实心数字压死整页，而它照样渲染）；**序号只能 1–2 位**（三位数右半截钻到标题底下再被 `overflow:hidden` 静默裁掉）；**标题 8 个汉字以内**（换第二行会压到底部那条细线上）；**底部只能 3 格**（第四条换到…

L42 arc-stage-metrics（半圆舞台：底部大半圆 + 弧内三组数据 + 顶部居中标题）
归属：内容 / 章节 / 结尾 · 形状：数据
适用：三个并列的关键指标 / 三项成果 / 三个覆盖面，要有仪式感（开场成绩单 / 章节前的三个数 / 结尾三项承诺）；文字很少（标题 + 一句引言 + 3 组数据，约 160–220 字）。
结构：整库唯一的**弧形舞台** —— 底部中央直径 1440px 的暗色半圆（`border-radius:720px 720px 0 0`，内可放压到 `.5` 的气氛图 + 上浅下深黑渐变）从底边升起，外套一圈大 20px 的 1px 细线弧；3 组「56px 品牌色数字 + 一行白字说明」按 150° / 90° /…
图槽位：1 个（`pXX_stage`，可选，concept 模式 16:9 **远景/夜景/肌理，主体别靠左右两端**（被圆角切掉）；**不配图也完整**（纯深色半圆，V4））。
fullbleed：是
提示：**三组坐标是算出来的**，改了 `.l42-stage` 的宽高必须重算 `.l42-m1/m2/m3` 的 `left/top` —— 白字一旦落到弧外面的米底上就**直接看不见**，而 HTML / CSS / `checkPage` 一个字都不说；**圆角必须写 `720px 720px 0 0`、不能写 `5…

L43 stacked-tilt-cards（层叠微旋转卡片：3 张带投影的图文卡，各自歪一点）
归属：内容 · 形状：并列
适用：三个案例 / 三个交付物 / 三张实拍图 + 说明，要"有手感"而不是"整齐"（作品集 / 案例三选 / 交付样张）；文字容量中等偏大（3 张各「小标签 + 小标题 + 2–3 行」，约 300–380 字）。
结构：整库唯一的**层叠微旋转** —— 标题 + 引言之下，3 张 460px 宽白卡横排、彼此**负 16px 叠压**，各自 `rotate(-3° / +1.6° / -1.2°)` + `0 26px 60px` 大投影，中间那张 `z-index:2` 压在两侧上面；卡内四层「4:3 图 / 小标签 / 小标题 …
图槽位：3 个（`pXX_c1..c3`，可选但**要么三张全有要么整页不放**（只有一两张有图时三张卡高度差一截，层叠就散了），case 模式 4:3 **主体居中、构图简单、三张同一路调性**）。
fullbleed：否
提示：**只能 3 张**（`460×3-16×2=1348` 还在 1640 内容区里，第四张起总宽 1808 > 1640，最右那张被裁掉一半而前三张排得整整齐齐）；**叠压别超过 -16px**（卡内 padding 只有 30px，加上 ±3° 旋转带来的十几像素偏移，再叠就盖住下一张正文的左端一两个字，而那几行看起…

L44 vertical-timeline-five（竖向时间轴：左侧说明栏 + 右侧 5 个节点）
归属：内容 · 形状：时序
适用：5 个阶段的推进路线 / 实施节奏 / 里程碑 / 发展沿革，每一步都要写一两句而横向轴放不下；文字容量中等偏大（左栏标题 + 引言 5–7 行 + 贴底一句，右栏 5 节点各「阶段标签 + 小标题 + 1–2 行」，约 450–560 字）。
结构：整库唯一的**竖向**时间轴（此前时序只有 L6 横向轴 / L25 横向节点）—— 左 460px 说明栏（54px 衬线标题 + 引言 5–7 行 + `margin-top:auto` 贴底一句）+ 右侧一条通高 2px 细线，5 个 24px 空心圆点各在自己那一格（`flex:1`）**正中**，最后一个点实…
图槽位：0 个（**这一条不放图** —— 五个节点占满右半，图只能塞进左栏那 460px 变成一张看不出内容的小图；要配图的时序页用 L25 / L28）。
fullbleed：否
提示：**轴线两端那个 `10%` 是按 5 个节点算的**（`1/(2×5)`），节点数一变必须跟着改（4 个 → `12.5%`）—— 不改的话轴线两端各穿出首尾圆点 21px，看起来像"轴画长了"，而每一条节点都完整、一处都不报错；**节点只能 4–5 个**（6 个起每格压到 140px 以下、小标题和说明挨在一起看…

L45 data-table-sheet（数据表页：报告风标题条 + 全宽数据表 + 结论条）
归属：内容 · 形状：数据
适用：**整库唯一的真表格页** —— 报价/预算表、参数对照表、分项指标表、排期表、评分表；任何「几列口径 × 几行对象」的内容（此前只能硬塞进「几个大数字」那一档或图文行表，一塞进去列口径就没了）。文字容量中等（表头 3–6 列 + 表体最多 8 行，每格 10 字以内，约 300–500 字）。
结构：报告风标题条（`.rp-head` + 可选 `lead`）→ 右对齐的**单位/口径行**（`.dt-unit`）→ **全宽 `<table class="dt-table">`**（深底反白表头 + 偶数行白底斑马纹 + 首列加粗 + 数字列 `class="num"` 右对齐等宽 + 最多一行 `<tr cla…
图槽位：0 个（**这一条不放图** —— 表格已经占满内容区，塞图只能把列压窄到换行；要图文对照的清单换成带缩略图的行表那一档）。
fullbleed：否
提示：**表体最多 8 行**（含合计行；实测留给表体 555px、一行 62px。第 9 行起表格从 `.dt-wrap` 下沿漏出去压在脚注和结论条上，出来是字叠字的一页 —— 去掉 `lead` 和脚注能到 9 行，十几行必须拆两页）；**列数 3–6**（7 列起每列不到 220px，表头那几个词各自换两行，行高被顶…

L46 prose-sections-numbered（分节说明页：报告风标题条 + 2–3 个「编号 + 小标题 + 整段正文」白底区块）
归属：内容 · 形状：并列
适用：**整句成段的文字页** —— 方案概述 / 核心功能 / 预期效果 / 服务内容 / 实施要点 / 风险与应对，提案里那种「几个小标题，每段三五句话」的页；文字容量大（标题 + 引言 + 2–3 节各「编号 + 小标题 + 一段整话」，三节约 450–700 字）。要点式清单不要用这一条。
结构：报告风标题条（`.rp-head` + 建议带 `lead`）→ 竖着排的 2–3 个白底区块（`.l46-list` > `.l46-item`，`var(--c-card)` 底 + 一圈细线 + 左侧 3px 品牌色竖条），每块是 `92px 1fr` 两列：左列一个 44px 品牌色编号，右列「小标题（`<h3…
图槽位：0 个（**这一条不放图** —— 三个区块横向占满内容区，图只能塞进区块里把正文挤成窄栏；要每节配图的清单换成带缩略图的行表那一档）。
fullbleed：否
提示：**区块只能 2–3 个**（`.l46-list` 是 `flex:1`，高度被剩余空间钉死而**不裁**内容 —— 第 4 个区块从下沿漏出去压在结论条上，出来是字叠字的一页）；**三节时正文合计 ≤ 8 行**（实测：留给区块的是 640px，每块固定开销 97px + 间距 24px，正文一行 35px 约 5…

L47 bar-chart-rows（图表页：报告风标题条 + 横向条形 4–7 行 + 结论条）
归属：内容 · 形状：数据
适用：**几个数比长短** —— 耗时/预算/占比分布、渠道效果对比、几项指标横向比大小（4–7 行，每行「名称 + 条 + 数值」，约 150–300 字）。名称长、或者要的就是「谁最长谁最短」的排序，走这一条；**按时间/区间顺序看一个量怎么变**用竖柱那一条（L52）。只有 3–6 个孤立大数字、或要横向纵向对着读的矩阵，都不用这一条。
结构：报告风标题条（`.rp-head` + 建议带 `lead` 说清在比什么、按什么排）→ 右对齐单位/口径行（`.dt-unit`，与表格页共用）→ `.l47-rows` 里 4–7 个 `.l47-row`，每行 `300px 1fr 190px` 三列：左列名称（可挂一段另起一行的灰色 `<em>` 英文/口径）…
图槽位：0 个（**这一条不放图** —— 条形已经占满内容区，塞图会把轨道压到两三百像素宽，条与条的长短差就看不出来了；**图表截图也禁用**，有数据就用这一条画）。
fullbleed：否
提示：**条长绝对不许自己折算百分比** —— 每行 `--bar-v` 就写**右边印出来的那个原始数**，除法交给浏览器的 `flex-grow`（自己算错的那一条是一根颜色正常、长度看起来很合理的条，旁边的数字还是对的，一处都不报错，而读的人得出的结论是反的）；**轨道上限 `--bar-max` 不要写，由代码算**…

L48 agenda-offset-card（目录页：底部品牌色带 + 上方错位白卡 + 两列章节条目）
归属：章节 · 形状：并列
适用：**默认停用**（migration 103 —— 稿子里不排 agenda 页了）。要用得先在案例库页面上手动启用；原来的用法是整份的目录页 / 议程页 / 「今天讲三件事」，浅底、文字为主，4–8 条章节。**不当内容页用**（一条只放得下一句话）。
结构：**横向错位叠压** —— 品牌色实带贴底边通宽（`.l48-band`，高 38%）→ 一张白卡从上方压下来盖住它的上半（`.l48-card`：`top:150px` / `bottom:90px` / 左右各 `var(--pad-x)`，带大阴影），于是色带只在底部露出一条 90px、左右各露 140px → …
图槽位：0 个（**这一条不放图** —— 色带和白卡已经把画面分完，卡里塞图只能占掉一格条目，出来是「少了一章」的目录）。
fullbleed：是
提示：**错位是结构签名**（色带贴底 + 白卡往上错，露出底 90px 和左右各 140px；把卡片改成贴边之后色带一条都看不见，这一页退回「一张白卡列了几条」，而每一层都渲染正常）；**卡片 `top` 不许小于 150px**（代码贴的模块名占 44–100px 那条带，卡片顶上去之后蓝字压在白卡边沿上，看起来只是「有…

L49 closing-next-steps（结尾页：浅底 + 右下角出血品牌色块 + 往左上错位的白卡「下一步」）
归属：结尾 · 形状：聚焦
适用：整份最后一页，而且这一页要留下**接下来做什么** —— 提案的下一步 / 试点安排 / 需要对方决策的三件事；浅色稿子的收场（约 150–300 字）。纯致谢、纯金句的收场不用这一条。
结构：**斜向错位叠压** —— 品牌色实块占右下角（`.l49-block`，40%×52%，右边和下边都出血）→ 一张白卡从左上方压下来盖住它的左上角（`.l49-card`：`top:400px` / `right:96px` / `bottom:96px` / `width:38%`，带大阴影），色块只在右侧露出 9…
图槽位：0 个（**这一条不放图** —— 色块、白卡、巨字三层已经把画面分完，图必然是压在色块上的第四层，斜向错位就读不出来了）。
fullbleed：是
提示：**只当整份最后一页**（中间用它会读成「讲完了」）；**这一页的重点是「下一步做什么」**，纯致谢/纯金句的收场用暗底那一条，**同一份里两条结尾只能选一条**；**错位是结构签名**（色块占右下角两边出血 + 白卡往左上错，色块只露右 96px、底 96px；把色块铺满右半、或把卡片挪进色块里之后，这一页退回「浅底…

L50 stacked-layers-arch（分层架构页：右侧通高品牌色块 + 从左侧压过来的 3–4 条层带）
归属：内容 · 形状：并列
适用：**技术/产品架构分层**（应用层-能力层-数据层）、能力地图、平台由哪几层拼起来、组织分层；**整库唯一的架构/分层图**（3–4 层，每层 2–5 个要素块，全是短词，约 100–200 字）。层与层是包含/支撑关系，先后步骤用时间轴那几条。
结构：**整组错位叠压** —— 品牌色实块贴右边缘通高（`.l50-block`，32%）→ 3–4 条层带（`.l50-layers`：`left:var(--pad-x)` / `right:96px`）从左侧压过来盖住它的左侧大半，色块只在右边露出 96px、上下各露一截 → 每条层带 `280px 1fr` 两列：…
图槽位：0 个（**这一条不放图** —— 层带铺满整个内容区，图只能压在色块上、把唯一那块品牌色盖掉；**现成架构图截图也禁用**，有分层就用这一条画）。
fullbleed：是
提示：**只当分层/包含关系用**（画成先后步骤时读的人会以为下面那层支撑上面那层）；**色块必须在右边**（贴左的话代码贴的模块名 —— 左 140px、44–100px 那条带 —— 压在品牌色实底上，蓝字对比度不够而它照样渲染）；**层带别收进内容区**（`right:96px` 是叠压关系的全部，改成 `var(--…

L51 tier-plans-trio（三档方案页：报告风标题条 + 三列方案卡，推荐那档挂一面小旗）
归属：内容 · 形状：对比
适用：**报价/合作分档**（试点-落地-全量）、产品版本对比（基础-专业-企业）、服务档位、三条路线各自的代价 —— 凡是「三个选项，其中一个是我们建议的」（三列各 2–4 条包含项，约 200–350 字）。两个选项对立用对比双卡那几条，四档起用表格页那一条。
结构：报告风标题条（`.rp-head` + 建议带 `lead` 说清三档的差别在哪一件事上）→ 三列等宽方案卡（`.l51-trio`，`1fr 1fr 1fr`），每列：英文小字 + 档位名 + 一个大数字（单位进 `<i>`）+ 2–4 条包含项（每条上方细线）+ 贴底的「适合谁」→ 底部结论条（`.rp-foot`…
图槽位：0 个（**这一条不放图** —— 三列已经等分了整个内容区，卡里塞图会挤掉两条包含项，出来是「这一档少了两项」而三列都还渲染正常）。
fullbleed：否
提示：**三列一模一样，推荐哪一档只由那面 `.l51-flag` 小旗说** —— 小旗必须挂一面、且只挂一面（一面都不挂的话「三个选项里我们建议哪个」这一页一个字都没说，而三列各自都很完整、读起来只是「让对方自己挑」）；**别给某一列套满色底/负 margin 的 inline style**（那一列在整份浅色稿子里是唯…

L52 column-chart（图表页：报告风标题条 + 4–8 根竖柱 + 结论条）
归属：内容 · 形状：数据
适用：**按顺序看一个量怎么变** —— 逐月/逐季/逐年的营收、五期投放的转化、四个阶段的耗时、几档价格区间的分布（4–8 根柱，每根「柱 + 柱顶数字 + 柱下名称」，约 120–260 字）。横轴是顺序（时间/区间/流程先后），**柱子不许按大小重排**；名称长或者要比排序用横条那一条（L47），要横向纵向对着读的矩阵用表格页。
结构：报告风标题条（`.rp-head` + 建议带 `lead` 说清在看什么、横轴是哪几期）→ 右对齐单位/口径行（`.dt-unit`，与横条页、表格页共用）→ `.l52-plot` > `.l52-cols` 里 4–8 个等分的 `.l52-col`：每根是 `.l52-track`（上 `.l52-gap` +…
图槽位：0 个（**这一条不放图** —— 柱子要占满内容区高度才看得出高低差，塞图会把轨道压到两三百像素高，几根柱的差别就没了，而那一页看起来只是「图小了点」；**图表截图也禁用**，有数据就用这一条画）。
fullbleed：否
提示：**柱高绝对不许自己折算** —— 每根 `--bar-v` 就写**柱顶印出来的那个原始数**，除法交给浏览器的 `flex-grow`（自己算错的那根是一根高度看起来很合理的柱、柱顶数字还是对的，一处都不报错，而结论是反的）；**轨道上限 `--bar-max` 不要写，由代码算**（`chartData.ts`，…

L53 line-chart（图表页：报告风标题条 + 5–8 个点的折线 + 结论条）
归属：内容 · 形状：数据
适用：**一个量沿着时间连续地走** —— 半年的月度营收、12 周的留存、连续几期的转化率、一条指标的爬坡曲线；读的是**趋势和拐点**（哪一段陡、哪里掉头），不是「谁比谁大」（5–8 个点，约 120–260 字）。**离散类别（渠道/城市/产品线）绝对不能连成线**（那条线会让人以为两个类别之间有过程），那种数据用横条页；「每一期各自多少」用竖柱页（L52）。
结构：报告风标题条（`.rp-head` + 建议带 `lead` 说清这条线是什么指标、横轴是哪一段）→ 右对齐单位/口径行（`.dt-unit`）→ `.l53-plot` 里**两层叠在一起**：`.l53-cols`（等分 5–8 个 `.l53-col`，每格一个 `.l53-dot` 圆点 + `.l53-val…
图槽位：0 个（**这一条不放图** —— 折线要占满内容区高度斜率才读得出来，塞图会把绘图区压到两三百像素高，一条陡线看起来只是「有点起伏」；**图表截图也禁用**，有数据就用这一条画）。
fullbleed：否
提示：**一个坐标都不要自己算** —— 只写 `--bar-v`（点上印出来的那个原始数），`--pt-y` 和 polyline 的 `points` 全由代码算（`chartData.ts`）：两处各算一遍的话线从点旁边穿过去，按 `i*100/(n-1)`（0 / 25 / 50 / 100 那种一眼看着对的写法）算…

L54 donut-chart（图表页：报告风标题条 + 左环右图例的 3–6 块占比 + 结论条）
归属：内容 · 形状：数据
适用：**一个整体拆成几块、几块加起来就是全部** —— 收入按业务线的构成、预算分配、客户按行业的分布、流量来源占比；读的是**「谁占大头 / 结构是不是集中」**（3–6 块，约 120–260 字）。**加起来不是一个整体的数绝对不许画成环**（三个城市的营收、四家友商的估值 —— 环本身在说「这几块凑成了全部」），那种数用横条页（L47）；比高低用横条页、按时间走用折线页（L53）或竖柱页（L52）。
结构：报告风标题条（`.rp-head` + 建议带 `lead` 说清这一整圈是什么口径下的 100%）→ 右对齐单位/口径行（`.dt-unit`）→ `.l54-plot` 左右两块：左边 400px 的 `.l54-ring`（环那一圈是**一句 conic-gradient**，中间 `.l54-hole` 挖洞放…
图槽位：0 个（**这一条不放图** —— 左边那 400px 已经是这一页的视觉主角，再塞一张图就是两个主角抢位置，环被压小之后几块的差别看不出来，而那一页看起来只是「排得有点挤」；**图表截图也禁用**，有数据就用这一条画）。
fullbleed：否
提示：**一个角度、一个颜色都不要自己写** —— 只写 `--bar-v`（图例上印出来的那个原始数），环那一圈 `--ring` 和每块的 `--slice` 全由代码算（`chartData.ts`）：两处各算一遍的话图例上写着「蓝色 = 直营」而环上那块蓝的是分销，读的人把每一块都认成了别的东西，而那一页颜色、单位、…

L55 cover-mist-bilingual（封面：全幅气氛照 + 左侧竖线双语小字 + 衬线巨标题 + 中英副标题 + 落款）
归属：封面 · 形状：聚焦
适用：**整份第 1 页，主题带气氛/地域/意境**（文旅、康养、园区、城市更新、年度开篇），手上有一张**淡的远景照**（雾山、水面、园区远景）。文字全部左对齐、只占左 2/3，右 1/3 留给照片主体（约 60–110 字）。
结构：`.l55-wrap`（inset:0）里由后到前四块：`.l55-bg`（`<img>` 铺满 + `::after` 一层 **94deg 从左浓到右淡**的深蓝黑蒙版）→ `.l55-eyebrow`（左上，`border-left` 一道 2px 半透白竖线 + `<b>` 两行英文全大写 + `<span>`…
图槽位：1 个（`pXX_cover`，**必填** —— 没有图这一页就是一块深蓝底）：case、16:9，**远景气氛照、主体（塔/山/楼/水面）在右半**、左半是天/雾/水这种干净的淡区，整体偏淡偏冷。禁用人脸特写、带文字的图、主体居中的图。
fullbleed：是
提示：**蒙版那一层（`.l55-bg::after`）一句都不能去** —— 气氛照本身很亮（天、雾、水面），去掉之后白标题压在亮处，出来是一页「标题若隐若现」的封面，一处都不报错；方向也别反（文字全在左半，蒙版反过来等于压错了半边）；**照片主体必须在右半**（主体居中或偏左的图进来，标题压在结构线上那几个字就读不出来）…

L56 cover-corporate-caps（封面：亮调实景 + 中文口号 + 两行英文巨字 + 一排能力标签 + 深色署名块）
归属：封面 · 形状：聚焦
适用：**整份第 1 页，企业形象/实力类开场**（制造、装备、工程、供应链、品牌总览），主题能拆出 **3–5 个并列能力词**，手上有一张**亮调实景照**（厂区、车间、办公楼、产线）。文字全部深色、集中在左栏（约 60–120 字）。
结构：`.l56-wrap`（inset:0）里：`.l56-bg`（`<img>` 铺满 + `::after` 一层 **92deg 从左几乎不透明到 84% 处全透明**的浅底幕帘）→ `.l56-mark`（右上一行加宽字距英文标记）→ `.l56-main`（`top:50%` + `translateY(-50%…
图槽位：1 个（`pXX_cover`，**必填** —— 没有图这一页左边一列字浮在米底上）：case、16:9，**亮调实景（厂区/车间/办公楼/产线）、主体在右半**、天光充足。禁用暗调夜景（深色文字读不出来）、人脸特写、带文字的图。
fullbleed：是
提示：**这是 L55 的反面：亮照片 + 深色字，所以幕帘是浅色的** —— 换成深色蒙版之后深字压深底，出来是一页「文字若隐若现」的封面，一处都不报错；**照片主体必须在右半**（主体在左半的话它整块被幕帘洗白、右边空着，看起来像「这张图没加载好」）；**主角是那两行 82px 英文巨字，中文口号只有 32px —— 不…

L57 dual-table-sheet（双表页：报告风标题条 + 上下两张各带小标题和口径的表 + 结论条）
归属：内容 · 形状：数据
适用：**一段资料里其实是两组口径不同的数** —— 报价 + 工期、去年 + 今年、A 方案 + B 方案、投入 + 产出、线上 + 线下、硬件清单 + 服务清单。单表那一档只有一张表，两组数并进去之后**列头只能取一个口径**，另一组数被读的人拿错口径去套（「工期那两列的单位也是元？」），而那张表列数、斑马纹、合计行全都正常、一处都不报错 —— 所以这种内容要挑这一条。文字容量中等（两张表各 2–5 列 × 3–4 行，约 300–460 字）。**只有一组数的时候不要挑它**（下面那格空着，看起来像「这一页没排完」）。
结构：报告风标题条（`.rp-head`，标题说清两张表合起来在回答什么问题；**尽量不写 `lead`**）→ `.l57-stack` 里**上下两格** `.l57-block`（`flex:1` 均分），每格 = `.l57-cap`（左边 `<h3>` 说这张表是什么 + 右边 `.l57-unit` 是**这张表…
图槽位：0 个（**这一条不放图** —— 两张表已经占满内容区，塞图只能把列压窄到换行；**图表截图也禁用**）。
fullbleed：否
提示：**只能两张表**（第三个 `.l57-block` 进来之后三格均分、每格 200px 出头，三张表各自从下沿漏出去压在下一张的小标题上，出来是字叠字的一页 —— 第三组数拖到下一页）；**每张表最多 4 行**（含合计行；实测每格 315px，小标题 47 + 表头 48 = 95，一行 49px → 4 行剩 2…

L58 quote-photo-stamp（金句页：全幅实物特写 + 空心英文水印 + 巨幅居中宣言 + 巨型水印汉字）
归属：章节 / 结尾 / 内容 · 形状：聚焦
适用：**章节之间换一口气、或者结尾立一句主张**，而这句话有具体的物可以配图（产品、原料、现场、材料、场景局部）。整页只有**一句话**（约 20–60 字）。
结构：`.l58-wrap`（inset:0）里三层：`.l58-bg`（`<img>` 铺满 + `::after` 一层 **上浓（.62）→ 中淡（.4，44%）→ 下浓（.68）** 的深色蒙版）→ 两层水印（`.l58-echo` 左上一行 **172px 空心英文**：`color:transparent` + …
图槽位：1 个（`pXX_hero`，**必填** —— 没有图这一页是一块深灰底加一句话，和 L7 撞脸还少了底纹）：concept、16:9，**实物/意象特写**、主体填满画面、背景干净、中间偏暗。禁用远景（远景换 L59）、带文字的图、人脸特写。
fullbleed：是
提示：**整页只有一句话** —— 往里加要点列表、卡片、数据就把它变成一页普通图文页了，而画面完全正常；**蒙版一层都不能去**（实物特写有大片高光，白字压上去只是「若隐若现」，浏览器和 `checkPage` 都不报错）；**`color:transparent` 和 `-webkit-text-stroke` 必须成对…

L59 quote-scene-nodes（金句页：全幅远景 + 居中主张 + 一排 4–6 个圆形节点 + 底部英文小字）
归属：章节 / 结尾 / 内容 · 形状：聚焦
适用：**章节之间收束** —— 一句主张 + 「这句话由哪几件事支撑」；也可当结尾页（主张 + 几条承诺）。约 60–110 字。
结构：`.l59-wrap`（inset:0）：`.l59-bg`（`<img>` 铺满 + `::after` 一层 **上浓（.58）→ 中淡（.3，34%）→ 下浓（.72）** 的深色蒙版）→ `.l59-main`（画面纵向居中、`text-align:center`：`.l59-kicker` 一行加宽字距英文小…
图槽位：1 个（`pXX_scene`，**必填** —— 没有图这一页是一块深灰底，那排圆没有任何依托）：concept、16:9，**远景/全景**（山、海、天际线、园区全景），**上下要有大片平坦干净区**、整体偏暗。禁用特写（特写换 L58）、带文字的图、人脸。
fullbleed：是
提示：**蒙版必须是上下浓中间淡**（金句在中间、节点在下三分之一 —— 换成单向渐变之后下面那排圆的白字压在亮处读不出来，而那一页照样渲染）；**圆里只放文字（1 个汉字或 1–2 位数字），不放图标、不放 `<img>`**（库里没有图标字体，写 `<i class="icon-x">` 出来是一个空圆圈，画面上像「这排…

L60 arrow-lead-photo-strip（内容页：左列注记 + 右侧巨标题带箭头 + 通栏结论色带 + 底部通宽照片条）
归属：内容 / 章节 · 形状：聚焦
适用：**一页里同时有「一句主张 + 几个数 + 一句结论 + 一张现场图」** —— 项目开场、阶段小结、方案主推页。文字容量小（约 120–220 字）。**要摆并列要点或数据卡的时候不要挑它**（三段横切之间没有余量，塞进去就是字叠字）。
结构：`.l60-wrap`（inset:0、浅底）里**三段横切**：上段 `.l60-top`（`top:132px`，左 330px 的 `.l60-notes` 放 2–4 条注记「一个数 + 一行说明」，右 `1fr` 的 `.l60-head` 放 76px 衬线巨标题 + `.l60-arrow` 一条 CSS…
图槽位：1 个（`pXX_strip`，**必填** —— 没有图这一页底下 430px 是一块深灰，那排药丸标签和圆按钮悬在空处）：case、16:9，**横向条状裁切**（画面实际 1920×430，上下被 `cover` 切掉大半）：主体在**中间偏左**、上方留白。禁用带文字的图。
fullbleed：是
提示：**三段的 y 是写死的**（上段 132、色带 530、照片条 430 贴底），段与段之间没有余量 —— 挪任何一段都会盖住相邻那段，而三层都渲染正常（读起来只是「这一页排得挤」）；**注记 2–4 条、说明各一行**（一条 82px + `gap:22px`，上段只有 398px，**第 5 条直接顶到色带上**；…

L61 tint-panel-illus-wave（内容页：顶部浅调插画面板 + 波形下沿 + 左侧领句/两行标题/一段正文 + 底部并列小要点）
归属：内容 · 形状：分屏
适用：**要用一段话（而不是几条要点）把一件事讲清楚**，而这件事有**插画/示意图**可配 —— 产品说明、机制介绍、服务流程总述、原理页。文字容量中等（约 200–320 字）。**手上只有实拍照片时不要挑它**（插画位是 `contain` 摆的，照片会留两条空边）。
结构：`.l61-wrap`（inset:0、浅底）：`.l61-panel`（顶部通宽 702px 的**浅蓝调面板** `var(--hl-b)` + `overflow:hidden`）里放 `.l61-illus`（右侧 44% 的插画，`object-fit:contain`）和 `.l61-wave`（一块页面底…
图槽位：1 个（`pXX_illus`，**必填** —— 没有图面板右半是一块空的浅蓝，整页只剩左边一段话贴在角上）：concept、16:9，**插画/示意图**（线稿、等距图、结构示意），主体居中、背景干净、**浅色调**。禁用实拍照片（换 L1/L3）、带文字的图。
fullbleed：是
提示：**面板只能是浅底**（`var(--hl-b)` / `var(--hl-o)` 这类）—— 代码贴的那行模块名走 `.slide-header .kicker`、颜色写死在 `var(--c-accent-deep)` 上，而它落在 44–100px 也就是落在这块面板里，换成深色实底之后是深蓝压深蓝，**两处都在…

L62 team-portrait-rows（内容页：标题条 + 两列人像卡「左 3:4 人像 + 右 姓名/头衔/一句话」）
归属：内容 · 形状：并列
适用：**一页要摆好几个人** —— 项目组配置、专家/顾问团队、评审组、榜样人物、讲师阵容、合作方对接人、部门负责人。**3–8 人**（推荐 4 或 6，偶数最齐）。只有 1 个人换 L12；要摆的是「4 个地方 / 4 款产品」那种非人物卡换 L16。
结构：`.l62-wrap`（flex column，留白全由 `.slide-inner` 的 `--pad-*` 给，这一层不写 padding）：`.l62-head`（左 `h2.page-title` 42px 衬线标题 + 右 `.l62-lead` 一句灰色引言，下沿 2px 深线）→ `.l62-grid`（…
图槽位：3–8 个（`pXX_p1..p8`，**一人一张、必填** —— 缺一张那一格是一块 `var(--c-bg-alt)` 空框，右边的名字和头衔照旧在，看起来像「这个人没照片」而不像出错）：case 模式、**3:4 竖版**，**同一批人像**（同背景、同光、同景别、齐胸半身、正面平视，脸在上三分之一）。禁止混搭构图、全身照、带文字/水印的图。
fullbleed：否
提示：**人像那一格不许写宽度**（只留 `aspect-ratio:3/4`）—— 高度由行高撑出来、宽度按比例反算，这是「4 人和 8 人共用一份 CSS」的全部原因；写死宽度之后人数一变就是「图挤成一条」或者最后一行往画面下沿漏，而每一格单看都排得好。**别改成三列**（列宽 517px，人像照旧 273px 宽，文字…

L63 org-chart-tree（内容页：居中标题 + 顶层色块 + 2–4 条分支列「分支卡 + 下挂小项」+ 底部结论条）
归属：内容 · 形状：并列
适用：**要说清「谁管谁 / 谁向谁汇报」** —— 项目组织架构、组织体系、评审与决策链、部门职责分工、治理结构、供应链层级、平台功能分层。**分支 2–4 条**（3 条最稳），每条下挂 2–6 个小项。只是把几件事并排讲（**没有上下级关系**）换 L7/L16 那一族；一页摆人的照片和头衔换 L62。
结构：`.l63-wrap`（flex column，留白全由 `.slide-inner` 的 `--pad-*` 给，这一层不写 padding）：`.l63-head`（**居中** `h2.page-title` 40px 衬线 + 一行灰色副标）→ `.l63-top`（居中的品牌色块，`<b>` 最上面那一层 +…
图槽位：0 个（这一页要看的是关系，塞图进来会把连接线挤断）。
fullbleed：否
提示：**分支 2–4 条**（5 条起每列只剩 306px，组名换行、小项挤成三行，而每一格都渲染正常）；**每列小项 2–6 条**（一行字那档，一条 54px + 12px 间距、可用 382px）—— **7 条起最后一条压在底部那条结论线上**，不裁不报错，看着像「这一条是另起的一段」；**各列条数差 ≤2**（差…

L64 dual-track-timeline（内容页：报告风标题条 + 上轨卡片行 + 中间阶段轴 + 下轨卡片行 + 结论条）
归属：内容 · 形状：时序
适用：**同一条时间轴上有两方/两条线在并行推进** —— 实施排期里的「我方交付 / 贵方配合」、「线上 / 线下」、「产品 / 市场」、「建设 / 运营」、每阶段的「做什么 / 产出什么」。**阶段 3–5 段**（推荐 4）。只有一条线的时间轴换 L6 / L25 / L44；要画甘特那种「跨几个月的横条」不要选它（条的起止得在代码里算，硬规则 3）。
结构：`.l64-wrap`（flex column，留白全由 `.slide-inner` 的 `--pad-*` 给）：`.rp-head`（报告风标题条 + 可选 `.lead`）→ `.l64-body`（`justify-content:center`，把下面三行整体摆在页面中间）：`.l64-row` 上轨（左 …
图槽位：0 个（两排卡片 + 一根轴已经占满，塞图进来卡片只剩一行字）。
fullbleed：否
提示：**阶段 3–5 段**，且**上下两轨的卡片数必须和轴上的节点数一样**（少一张卡片时后面所有列往左挪一格，卡片和圆点整体错开，而每一格都渲染正常 —— 这一页看起来只是「排得有点歪」；这个阶段那条线真没事就放一张 `.l64-card.off` 空格）；**轨道名 120px、行间距 24px、`.l64-axis…

L65 toc-number-grid（目录页：「目录 / CONTENTS」标题条 + 两列大编号条目「幽灵编号 + 章节名 + 一行说明 + 底细线」）
归属：章节 · 形状：并列
适用：**默认停用**（migration 103 —— 稿子里不排 agenda 页了）。要用得先在案例库页面上手动启用；原来的用法是「没有能用的图」时的目录页 / 导览页，4–8 条（偶数最齐）。同族的另外三条：L17（左图右圆圈，≤5 章）/ L33（左行表右竖图）/ L48（品牌色带 + 错位白卡）。
结构：`.l65-wrap`（flex column，留白全由 `.slide-inner` 的 `--pad-*` 给）：`.l65-head`（左 `h2.page-title`「目录」46px 衬线 + 右 `<span>`「CONTENTS」宽字距，下沿 2px 深线）→ `.l65-grid`（**两列**，行高跟…
图槽位：0 个（这一条就是「没有图也能做目录」的那一档）。
fullbleed：否
提示：**4–8 条**（偶数最齐）：**奇数条最后一格空一个**（不裁不报错，看着像漏了一章）；**10 条时那条细线已经顶到编号脚下（差 5px），12 条起细线从编号上横穿过去** —— 字都在、一处都不报错，看起来像版式自带的删除线（章节真有 10 个以上就合并成大块）。**`on` 只能有一条**（两条以上等于告诉…
